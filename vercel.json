{
  "version": 2,
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter ./frontend build",
  "builds": [
    { "src": "frontend/next.config.mjs", "use": "@vercel/next" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "frontend/$1" }
  ]
}
